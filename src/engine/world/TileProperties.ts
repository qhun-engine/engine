/**
 * a base interface for engine internal tile properties
 */
export interface TileProperties {

    /**
     * indicates that this tile acts as collider for collision detection
     */
    blocked: boolean;
}
